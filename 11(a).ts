// Module
export class Point{            // This point is class

    constructor(x ?: number, y ?: number){        // making optional
    }

    draw(){
        console.log('X: ' + this.x + ', Y: ' + this.y);
    }
    
}